# kserve-nexus
kserve 管理平台


1. map 指向错误，map指向的和从首页点击跳转的页面为一个页面
2. 节点大小适当调大，优化展示样式
3. 上侧展示信息为整个服务的信息，不需要与节点联动
4. 点击节点详情，跳转到：https://cn.bing.com/?FORM=Z9FD1
5. 图区域能够放大和缩小


整体比较满意，有以下优化点
1. 明确一个概念：服务：一个逻辑概念，包含多个子服务；服务有三种类型：isvc、llmisvc、graph；首页列表展示的就是所有的逻辑服务；map页面展示的是逻辑服务的子服务的关系。
2. 确保map指向的和从首页点击跳转的页面为一个页面，包括路由也要一致
3. 去掉map页面的汇总模块

整体比较满意，有以下优化点：

1. 服务拓扑图：放大、缩小、复原icon排版有点问题，请优化
2. 优化边的样式：箭头显示总是有缺陷，

整体比较满意，有以下优化点：

1. 节点的复杂模式适当比简约模式要宽，确保标签展示不要有重叠，每行最多展示两个标签
2. 另外，确保标签文字不要越过标签的展示范围


增加一个新的页面，有服务拓扑图节点跳转到此界面，分为以下几部分：

1. 第一部分为一个列表
   1. 标题：{类型}:{name}；
   2. 列表，必然有两列，没有表头（将返回的第一行作为表头，着重显示），具体内容根据接口返回的信息确定
      1. 必然会有一个第一列为`Labels`的行，内容显示多个标签，优化标签的展示样式
2. 第二部分为一个列表
   1. 标题为：Conditions
   2. 列表，必然有两列，没有表头（将返回的第一行作为表头，着重显示），具体内容根据接口返回的信息确定
3. 第三部分为一个列表
   1. 标题为：Containers
   2. 列表，必然有两列，没有表头（将返回的第一行作为表头，着重显示），具体内容根据接口返回的信息确定
   3. 必然会有第一列为`Status`的行：包含三种样式：RUNNING、UNKNOW、ERROR（非前两种状态即为ERROR）
   4. 必然会有第一列为`Last State`的行：列表展示两列，具体内容根据返回的信息确定
   5. 必然会有第一列为`Environment`的行: 列表展示两列，具体内容根据返回的信息确定（第二列增加可复制的icon，用于复制内容）
   6. 必然会有第一列为`Mounts`的行：列表展示，表头为：Mount Path、from、I/O，表格内容根据返回内容确定
4. 第四部分为一个列表：
   1. 标题：Volumes；
   2. 列表，必然有两列，没有表头，具体内容根据接口返回的信息确定
5. 第五部分为一个列表：
   1. Events
   2. 列表，必然有两列，没有表头（将返回的第一行作为表头，着重显示），具体内容根据接口返回的信息确定


页面生成要求：
1. 布局优雅漂亮
2. 每个部分的样式要有所区别
3. 行内容可能比较长，要求能够优雅展示


节点详情页面优化，在基本信息列表页，首行增加一个按钮；点击按钮时，在页面右侧弹窗，显示一个yaml文件

首页优化，服务列表优化：

1. 表头根据接口返回的内容确定，其他内容根据返回内容确定

整体比较满意，有以下优化点：

1. 页面已经基本完成，现有的数据都是写死到文件里的，改成由接口获取数据，接口的host为：`http:\\localhost:5109`
2. 更改菜单栏颜色，有所区分


整体比较满意，有以下优化点：

1. isvc、llmisvc、graph 分为三个接口：`/kserve/isvc`、`/kserve/llmisvc`、`/kserve/graph`
2. 筛选项：namespace由接口`/kserve/namespace`获取；name由上述三个接口返回的name组成



整体比较满意，首页服务列表筛选项有以下优化点：namespace和name由接口`/kserve/namespace`获取；接口结构为：
```json
{
   "abc": ["nameA", "nameB", "nameC"]
}
```

整体比较满意，增加以下功能点：

1. map 页：根据isvc、llmisvc、graph 分别选择：`/kserve/isvc/{name}?namespace={namespace}`、`/kserve/llmisvc/{name}?namespace={namespace}`、`/kserve/graph/{name}?namespace={namespace}`接口获取数据
2. 筛选项：namespace和name由接口`/kserve/namespace`获取；接口结构为：
```json
{
   "abc": ["nameA", "nameB", "nameC"]
}
```