如果有skill可以使用，请优先使用skill完成新的需求的开发；如果没有对应的skill，请参考Ant组件文档，设计页面；

接口文档参见：.opencode/api.md
Ant Design 组件文档参见：.opencode/docs/ant_design.md

增加一个Dashboard页面布局如下：
1. 汇总数据（调用`Dashboard`接口获取数据）：
  1. isvc（总节点个数、正常节点个数、异常节点个数）
  2. llmisvc（总节点个数、正常节点个数、异常节点个数）
  3. graph（总节点个数、正常节点个数、异常节点个数）
  4. 在图标右上角，增加`?`icon，当鼠标悬浮时，显示说明信息
2. event事件列表：namespace name、apiVersion、kind、reason、message、count、lastTime
  1. 先调用：`获取websocket访问地址`接口， 获取链接信息：`data.url`
  2. 创建websocket链接，持续获取数据：只处理`data.type=event`的数据

要求：

1. 沿用现有的风格，设计页面

检查：

1. 是否编译通过
2. websocket 链接是否正常


如果有skill可以使用，请优先使用skill完成新的需求的开发；如果没有对应的skill，请参考Ant组件文档，设计页面；

接口文档参见：.opencode/api.md
Ant Design 组件文档参见：.opencode/docs/ant_design.md

优化修改：

1. 增加`汇总数据`与`event事件列表`模块之间的间隙
2. `event事件列表`：增加本地搜索项：namespace、kind、name，参见：.opencode/docs/components/table/demo/custom-filter-panel.tsx、.opencode/docs/components/table/demo/custom-filter-panel.md
3. `event事件列表`：增加本地排序项：count、lastTime，参见：.opencode/docs/components/table/demo/multiple-sorter.tsx、.opencode/docs/components/table/demo/multiple-sorter.md



如果有skill可以使用，请优先使用skill完成新的需求的开发；如果没有对应的skill，请参考Ant组件文档，设计页面；

接口文档参见：.opencode/api.md
Ant Design 组件文档参见：.opencode/docs/ant_design.md

新需求，对接isvc列表页：
1. 接口：获取isvc列表
2. 列表表头：name、namespace、deploymentMode、url、ready、prev、latest、prevRolledoutRevision、latestReadyRevision、age
3. 分页：每页最多20条数据
4. 筛选项：namespace
    1. 数据来源：`获取 namespace` 接口
    2. 呈现形式：下拉框的方式呈现获取到的namespace
    3. namespace允许为空；为空时，调用接口时，不传递此参数

要求：

1. 沿用现有的风格，设计页面
2. 页面美观



如果有skill可以使用，请优先使用skill完成新的需求的开发；如果没有对应的skill，请参考Ant组件文档，设计页面；

接口文档参见：.opencode/api.md
Ant Design 组件文档参见：.opencode/docs/ant_design.md

1. 接口：`获取isvc详情`
2. 参数获取：从`isvc列表页`进入，在`isvc列表页`name列添加超链接，并获取对应的参数
3. 简介信息：请设计合理的ui用于展示：name、namespace、deploymentMode、url、ready
4. `data[].conditions[]` 展示：列表形式，表头为：type、status、reason、message、severity、lastTransitionTime
5. `data[].events[]` 展示：列表形式，表头为：namespace name、apiVersion、kind、reason、message、count、lastTime
6. `data[].graph` 展示：这部分数据为节点的关系图，请使用G6组件展示，文档参见：https://g6.antv.antgroup.com


要求：

1. 布局顺序，从上到下：简介信息、`data[].conditions[]`、`data[].events[]`、`data[].graph`
2. 沿用现有的风格，设计页面
3. 页面美观
4. `data[].conditions[]`、`data[].events[]`：要使用不同的UI，有区分度：参见.opencode/docs/ant_design.md，选择合适的table样式
5. `data[].conditions[]`、`data[].events[]`：不需要分页



如果有skill可以使用，请优先使用skill完成新的需求的开发；如果没有对应的skill，请参考Ant组件文档，设计页面；

接口文档参见：.opencode/api.md
Ant Design 组件文档参见：.opencode/docs/ant_design.md

需求：

1. isvc 详情接口，增加返回yaml字段，需要在点击`查看YALML`按钮时，出现侧边栏，用来展示yaml数据

要求：

1. 宽度：yaml 浮窗宽度占屏幕的50%
2. 关闭方式：点击浮窗右上角的X按钮，或者点击浮窗外区域，浮窗关闭